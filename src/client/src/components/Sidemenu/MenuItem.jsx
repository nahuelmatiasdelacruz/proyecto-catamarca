import React from 'react'
import { MUI } from '../../helpers/MaterialUI'

export const MenuItem = ({title,icon}) => {
  return (
    <div className="menu-item">
        <MUI.Icons.AccountCircleIcon/>
    </div>
  )
}
